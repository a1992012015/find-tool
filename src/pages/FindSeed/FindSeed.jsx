import React, { Component } from 'react';
import { Form } from 'antd';

class FindSeed extends Component {
  render() {
    return (
      <div>This is content....</div>
    );
  }
}

export default Form.create()(FindSeed);
